# Rendered by Ansible into {{ kart_pds_data_dir }}/tranquil-pds.env (mode 0600).
# Mounted as env_file into the tranquil-pds container.

DATABASE_URL=postgres://tranquil_pds:{{ kart_pds_postgres_password }}@db:5432/pds
JWT_SECRET={{ kart_pds_jwt_secret }}
DPOP_SECRET={{ kart_pds_dpop_secret }}
MASTER_KEY={{ kart_pds_master_key }}
DISCORD_BOT_TOKEN={{ kart_pds_discord_bot_token }}
